import { Menu, X } from 'lucide-react';
function Header({nav,menu,setMenu}:{nav:(x:string)=>void;menu:boolean;setMenu:(x:boolean)=>void}){return <header className="header"><div className="shell nav"><button className="brand" onClick={()=>nav('home')}>WASCH<span>MATIK</span></button><nav className="desktop-nav"><button onClick={()=>nav('home')}>Startseite</button><button onClick={()=>nav('finder')}>Betrieb finden</button><button onClick={()=>nav('partner')}>Partner werden</button><button onClick={()=>nav('contact')}>Kontakt</button></nav><div className="nav-right"><button className="primary small desktop-cta" onClick={()=>nav('finder')}>Betrieb finden</button><button className="icon-btn mobile-only" onClick={()=>setMenu(!menu)} aria-label="Menü">{menu?<X/>:<Menu/>}</button></div></div>{menu&&<div className="mobile-menu"><button onClick={()=>nav('home')}>Startseite</button><button onClick={()=>nav('finder')}>Betrieb finden</button><button onClick={()=>nav('partner')}>Partner werden</button><button onClick={()=>nav('contact')}>Kontakt</button><button onClick={()=>nav('partner-login')}>Partner-Login</button></div>}</header>}

export default Header;
